
{% extends '/layouts/default.twig' %}


{% block content %}
    <a href="/shop/{{ product.shop_category_id }}">Назад</a>
    <h3>Товар</h3>
    <p>{{ product.name }}{{ product.id }}</p>
    <p>{{ product.price }}</p>
    <p>{{ product.description }}</p>
    <p>{{ product.status }}</p>
    <a href="#">Добавить в корзину</a>

{% endblock %}